# This file includes some additional build dependencies that were not caught by pybuild-deps.
# After updating this file, you need to run pip-compile and pybuild-deps. For example using uv:

# uv venv
# uv pip install pip-tools pybuild-deps
# uv run pip-compile --allow-unsafe --output-file=requirements-extra-build-deps.txt extra-build-deps.txt
# uv run pybuild-deps compile -o requirements-extra-build-deps-build.txt requirements-extra-build-deps.txt

# propcache requires Cython>=3.1.0 and <3.2.0
Cython==3.1.8
# typer-slim requires rich>=10.11.0
rich==14.2.0
